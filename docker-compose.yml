version: "2"
services:

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: CONTENEDOR_WEB_PARCIAL
    depends_on:
      - app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./web/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./web/certs:/etc/nginx/certs:ro
    networks:
      ejemplo_net:
        ipv4_address: 10.23.24.10
    restart: unless-stopped

  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: CONTENEDOR_APP_PARCIAL
    depends_on:
      - db
    volumes:
      - ./app/src:/var/www/html
      - ./app/apache-vhost.conf:/etc/apache2/sites-available/000-default.conf:ro
    networks:
      ejemplo_net:
        ipv4_address: 10.23.24.20
    restart: unless-stopped

  db:
    image: mysql:8.0
    container_name: CONTENEDOR_DB_PARCIAL
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: ejemplo_db
      MYSQL_USER: ejemplo_user
      MYSQL_PASSWORD: ejemplo_pass123
    volumes:
      - db_data:/var/lib/mysql
      - ./db/init:/docker-entrypoint-initdb.d:ro
    networks:
      ejemplo_net:
        ipv4_address: 10.23.24.10.30
    restart: unless-stopped

networks:
  ejemplo_net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.23.24.0/24

volumes:
  db_data: